services:

  - type: web
    name: manage-app
    runtime: image
    image:
      url: docker.io/kwsilence/manage-app:render
    plan: free
    envVars:
      - key: MANAGE_ROOT_USER
        value: admin
      - key: MANAGE_ROOT_PASS
        value: admin
      - key: DB_HOST
        fromDatabase:
          name: manage-db
          property: host
      - key: DB_PORT
        fromDatabase:
          name: manage-db
          property: port
      - key: DB_USER
        value: manageadmin
      - key: DB_PASS
        value: kws

databases:
  - name: manage-db
    databaseName: manage
    user: manageadmin
    plan: free